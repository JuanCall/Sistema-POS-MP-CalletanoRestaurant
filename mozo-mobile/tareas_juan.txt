Responsable: Juan (Solo)

Semana 1 (Cimientos):

Inicializar proyecto con expo init. Crear estructura de carpetas src/components, src/screens y src/services.

Crear src/screens/Login.js. Pantalla de acceso para mozos con validación de campos básica.

Crear src/services/authService.js. Lógica para conectar con el endpoint de login del Backend y guardar el token.

Configurar app.json. Definir iconos, nombre de la app y permisos de red para la tablet Android.

Crear src/components/MesaCard.js. Componente visual reutilizable para mostrar la mesa y su estado.

Semana 2 (Lógica Base):

Crear src/screens/Home.js. Vista principal con la cuadrícula de mesas obtenidas del Backend.

Crear src/screens/Menu.js. Interfaz de categorías (Entradas, Fondos, Bebidas) con botones grandes para dedos.

Crear src/components/CartItem.js. Componente para la lista lateral de pedidos donde se puedan añadir notas (ej: "Sin cebolla").

Crear src/services/api.js. Configurar instancia de axios con la IP del servidor local para obtener la carta de productos.

Implementar navegación con React Navigation entre la lista de mesas y el menú de pedidos.

Semana 3 (Tiempo Real):

Crear src/services/socket.js. Configurar el cliente de Socket.io para emitir el evento enviar-pedido al backend.

Implementar lógica en Menu.js. Al presionar "Enviar a cocina", emitir el socket y limpiar el carrito local.

Crear src/components/StatusBadge.js. Indicador visual en la tablet que muestre si el pedido ya fue recibido por la cocina.

Implementar manejo de estado con Context API o Redux para mantener el pedido vivo si el mozo cambia de pantalla accidentalmente.

Configurar alertas de "Conexión Perdida" si la tablet se aleja mucho del router Wi-Fi.

Semana 4 (Integración):

Crear src/screens/Settings.js. Opción para que el mozo cambie la IP del servidor manualmente si el router cambia de dirección.

Realizar pruebas de usabilidad táctil con el personal real del restaurante para ajustar el tamaño de los botones.

Optimizar la carga de imágenes del menú para que la app no se cuelgue en tablets con poca RAM (3GB).

Generar el archivo APK de prueba (expo build:android) para instalación directa en las tablets de Calletano.

Documentar el flujo de "Toma de pedido hasta envío" en el manual de usuario móvil.